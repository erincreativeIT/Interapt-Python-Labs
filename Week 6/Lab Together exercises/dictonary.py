# Dictinary comprehnsions!
# The : and { } are important!
a_dict = { key:key*key for key in range(1, 5) }
#What's this look like?
print(a_dict )
a_dict = {}
for key in range(1,5):
    a_dict[key] = key * key

    exit()

key_var1 = "key1"
key_var2 = "key2"
